import { domain } from '../domain';

export const setControls = domain.createEvent('setControls');
export const resetControls = domain.createEvent('resetControls');
export const reset = domain.createEvent('reset');
